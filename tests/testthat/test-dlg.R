context("Dialogs-General")

describe("dlg_dir", {
  #it("returns the default dir in non interactive session", {
  #  expect_is(dlg_dir(title = "test")$res, "...")
  #})

  #it("raises an error in non-interactive mode", {
  #  expect_error(dlg_dir(title = "test"),
  #    "No workable method available to display a dir selection dialog box using",
  #    fixed = FALSE)
  #})
})
